# app/

Placeholder. This directory will hold the Flutter Android client, built in a
later phase. See `docs/SPEC.md` section 6 for the mobile behaviour it must
implement, and `CLAUDE.md` for the invariants it must respect (foreground
service, offline queue, shift-gated tracking).

No Flutter project exists here yet.
